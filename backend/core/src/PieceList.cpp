#include "PieceList.h"
/**
 * List of pieces picked by the user.
*/



/**
 * Initialise the list of pieces for the given piece selection.
*/
PieceList::PieceList(Json::Value::Members selection) {
    // Iterate through keys
    for(auto const& key : selection) {
        
    }
}

